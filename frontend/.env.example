# =============================================================================
# LibSQLite Frontend Configuration
# =============================================================================

# Public API URL for client-side browser requests (optional if using proxy)
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Internal API URL for Next.js server-side reverse proxy route
INTERNAL_API_URL=http://backend:5000/api/v1
